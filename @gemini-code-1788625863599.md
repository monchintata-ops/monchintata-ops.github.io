### Orden de Comando para la IA en Visual Studio Code

Copia y pega esta instrucción en el chat de tu IA en VS Code:

```text
Lee el archivo AUTOMATIC_PIPELINE_MARKETPLACE_SPEC.md y aplica el flujo de procesamiento 100% automático:

1. Modifica el modal de "Agregar nuevo diseño" para que únicamente pida 1 archivo: "Archivo de impresión HD (.png / .svg)". Elimina los demás campos de archivos manuales.
2. En la API del servidor, usa Sharp para generar automáticamente:
   - La vista previa con marca de agua diagonal incrustada (opacidad 30%, max 1000px, formato WebP).
   - El asset liviano y transparente para el visor Canvas de mockups.
3. Asegúrate de que el archivo HD original quede guardado en la carpeta/bóveda privada de la tienda.
4. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para verificar que compila sin errores.
5. Haz git add, commit y push a origin main.


-------

¡Excelente decisión! Ir directo al **flujo 100% automático** es la mejor opción para la usabilidad del sistema: ahorra tiempo al subir productos, elimina errores humanos y protege de verdad tu catálogo y el de tus diseñadores colaboradores.

---

### ¿Cómo funcionará exactamente a partir de ahora?

1. **Carga en 1 Clic (Vista de Subida):**
* El diseñador o tú únicamente seleccionan **UN SOLO ARCHIVO**: `Archivo de Impresión HD (.png / .svg)`.
* El formulario no pedirá nada de previews ni imágenes de mockup.


2. **Procesamiento Inteligente en Servidor (Backend con Sharp):**
* **Bóveda Privada:** El archivo HD limpio original va directo a la carpeta protegida para descargas post-pago.
* **Preview Pública:** Crea una versión comprimida `.webp` de máximo 1000px y le incrusta encima la marca de agua (mosaico diagonal con 30% de opacidad) para que nadie pueda robar la imagen limpia.
* **Asset para Mockup:** Genera automáticamente la versión liviana y transparente optimizada para renderizarse al instante sobre la taza, el termo y las camisetas en el visor Canvas.



---

### Especificación Final para VS Code (`AUTOMATIC_PIPELINE_MARKETPLACE_SPEC.md`)

Crea este archivo en la raíz de tu proyecto para que la IA aplique el flujo automatizado:

```markdown
# Implementación del Flujo 100% Automático de Carga y Procesamiento

## Objetivo
Dejar un único campo de carga de archivo en el modal "Agregar nuevo diseño" y automatizar por completo la generación de vistas previas con marca de agua y versiones de mockup en el backend.

---

## 1. Simplificación del Frontend (`components/AddDesignModal.tsx`)

- Mantener **únicamente** el campo de carga: `Archivo de impresión HD (.png / .svg)`.
- Eliminar por completo los inputs manuales de `Vista previa (.webp)`, `Diseño para Mockup` y `Marca de Agua`.
- Agregar un estado de carga con spinner: *"Procesando resolución, generando vista previa protegida y preparando mockup..."*.

---

## 2. Pipeline Automatizado de Imágenes (`pages/api/designs/upload.ts` o equivalente)

Procesar el buffer del archivo de impresión principal mediante `sharp`:

1. **`original_file` (Bóveda):** Guardar el PNG a 300 DPI en el almacenamiento privado (`archivos-privados`).
2. **`preview_url` (Catálogo Público):**
   - Redimensionar la imagen a un máximo de `1000px`.
   - Inyectar marca de agua en mosaico diagonal (usando el logo de la tienda o del diseñador) a `30%` de opacidad.
   - Guardar en formato `.webp` optimizado.
3. **`mockup_url` (Visor Canvas):**
   - Redimensionar a `1000px` manteniendo transparencia limpia, pero reducida en peso para consumo ágil del Canvas 3D (camisetas, tazas y termos).

---

## 3. Verificación
1. Validar tipos de TypeScript: `.\node_modules\.bin\tsc.cmd --noEmit`.
2. Confirmar commit y push a `main`.

