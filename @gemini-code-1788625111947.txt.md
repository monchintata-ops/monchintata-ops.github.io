### Orden de Comando para la IA en Visual Studio Code

Copia y pega este prompt en el chat de tu IA en VS Code:

```text
Lee el archivo PROTECT_PREVIEW_WATERMARK_SPEC.md y aplica la protección de marca de agua en las vistas previas:

1. En la API que sirve las imágenes de vista previa (`/api/preview/...`), procesa la imagen al vuelo o al momento de la subida con Sharp para incrustar la marca de agua (watermark) en mosaico/diagonal con opacidad del 30%.
2. Asegúrate de limitar el tamaño de la preview a un máximo de 1000px para evitar que entregue la matriz completa de impresión a 300 DPI.
3. Verifica que la carpeta/bucket de archivos de alta resolución sea privada y solo entregue enlaces mediante validación de pago.
4. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para validar que todo compile sin errores.
5. Realiza git add, commit y push a origin main.