Lee el archivo PREVIEW_AND_MOCKUP_PROCESSING_SPEC.md y aplica los cambios solicitados:

1. En la generación de la imagen de vista previa (preview):
   - Agrega un fondo sólido oscuro (#1E293B o similar) en lugar de dejar la transparencia suelta.
   - Aumenta la presencia/opacidad de la marca de agua (50%-60%) para proteger el diseño.
2. En la carga de archivos:
   - Permite la subida de la marca de agua en su calidad original.
   - Permite la subida de la imagen para mockup y haz que el servidor le baje la resolución automáticamente para que el visor Canvas cargue rápido en prendas, termos y tazas.
3. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para comprobar que compila correctamente.
4. Al finalizar con éxito, ejecuta automáticamente en la terminal:
   git add .
   git commit -m "feat: fondo en preview, marca de agua visible y compresion automatica de mockup"
   git push origin main

```


Entendido perfectamente. Vamos a ajustar el comportamiento del formulario y el procesamiento de imágenes según estos 3 requerimientos exactos:

1. **Fondo para la imagen Preview:** Aplicar un fondo (color neutro/oscuro suave, por ejemplo `#1e293b` o `#0f172a`) a la imagen de vista previa para evitar el fondo transparente flotante y darle un aspecto de presentación profesional.
2. **Marca de agua más marcada:** Aumentar la visibilidad/opacidad y escala de la marca de agua en la preview para evitar la extracción limpia del arte.
3. **Imágenes para Marca de Agua y Mockups:**
* La plataforma permitirá **subir la imagen para Marca de Agua** (manteniendo su calidad original de logo para superponerla).
* La plataforma permitirá **subir la imagen para Mockups**, y la misma web se encargará de **bajarle la resolución automáticamente** a un peso liviano e ideal para el visor interactivo de prendas, tazas y termos.



---

### Especificación para VS Code (`PREVIEW_AND_MOCKUP_PROCESSING_SPEC.md`)

Crea o actualiza este archivo en la raíz de tu proyecto:

```markdown
# Ajuste de Procesamiento de Vista Previa, Fondo, Marca de Agua y Mockups

## Objetivo
Agregar fondo sólido a la vista previa del producto, hacer más visible la marca de agua y gestionar la bajada automática de resolución para el archivo dedicado a mockups.

---

## 1. Ajustes en el Formulario y Backend

- **Carga de Marca de Agua:** Permite subir el logo/sello PNG que se usará como marca de agua. No se baja resolución a este asset para mantener la nitidez del branding.
- **Carga de Imagen para Mockup:** Se sube la imagen transparente para el visor Canvas. El servidor le **baja automáticamente la resolución** (ej. máximo 800px-1000px, WebP/PNG optimizado) para que cargue ultra rápido en tazas, termos y camisetas.

---

## 2. Generación de la Vista Previa (Sharp Backend)

Al procesar la vista previa del producto (`preview_url`):

1. **Fondo Sólido:** Aplicar un fondo plano oscuro elegante (ej. `#1E293B` o `#0F172A`) debajo de la imagen transparente en lugar de dejar el fondo transparente sin relleno.
2. **Refuerzo de Marca de Agua:**
   - Incrementar la opacidad de la marca de agua al **50% - 60%**.
   - Escalar la marca de agua en mosaico o en diagonal más pronunciada para cubrir áreas clave del diseño.

---

## 3. Verificación y Despliegue Automático

1. Validar compilación de TypeScript: `.\node_modules\.bin\tsc.cmd --noEmit`.
2. Ejecutar comandos Git automáticamente:
   - `git add .`
   - `git commit -m "feat: fondo solido en preview, marca de agua reforzada y optimizacion automatica de mockups"`
   - `git push origin main`

