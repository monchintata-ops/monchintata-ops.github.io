Lee el archivo FIX_PREVIEW_CANVAS_SPEC.md y repara la vista previa para el dominio de producción main:

1. Corrige la API/endpoint que genera las imágenes (`/api/preview/...`) para que no fallen los buffers al procesar con Sharp.
2. Asegúrate de que las vistas previas se generen a 450px a proporción, con fondo plano oscuro (#1E293B) y la marca de agua bien visible (50% opacidad).
3. Añade la cabecera 'Cache-Control': 'no-store, must-revalidate' para obligar a Vercel a refrescar las imágenes en `creacionarte-store-git-main-monchintata-ops.vercel.app`.
4. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para verificar que compila correctamente.
5. Al finalizar con éxito, ejecuta automáticamente en la terminal:
   git add .
   git commit -m "fix: reparacion integral de api preview y renderizado de imagenes"
   git push origin main