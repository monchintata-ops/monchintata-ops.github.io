# Especificación: Manejo Seguro de Columna diseno_mockup_url en Supabase

## Objetivo
Garantizar que las funciones del panel de administración (incluida la acción "Limpiar duplicados") manejen de forma opcional o segura el campo `diseno_mockup_url` para evitar bloqueos por esquema de base de datos no actualizado.

---

## 1. Ajuste SQL Requerido
- Ejecutar la migración SQL en Supabase:
  ```sql
  ALTER TABLE productos ADD COLUMN IF NOT EXISTS diseno_mockup_url TEXT;