### Prompt Directo para la IA en Visual Studio Code

Copia y pega este prompt en el chat de tu IA en VS Code:

```text
Lee el archivo FIX_PREVIEW_GENERATION_SPEC.md y corrige el fallo de la vista previa:

1. Revisa la ruta/API de generación de preview (`/api/preview/...`) para evitar que devuelva la imagen fallback.
2. Asegúrate de que Sharp procese la imagen a 450px con el fondo sólido (#1E293B) y la marca de agua envueltos en un try/catch para capturar errores de buffer o tipo de archivo.
3. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para comprobar que compila correctamente.
4. Al finalizar con éxito, ejecuta automáticamente en la terminal:
   git add .
   git commit -m "fix: solucion a procesamiento de preview en sharp y manejo de errores"
   git push origin main


Ese problema ocurre porque Sharp o la API fallaron silenciosamente al procesar el primer archivo con los nuevos parámetros de 450px o marca de agua, rompiendo la ruta de la imagen (`preview_url`) y cayendo en la imagen *fallback* por defecto de CreacionArte.

Para solucionarlo y evitar que las imágenes rompan o queden inaccesibles, le indicaremos a la IA que añada un manejo de errores robusto (`try/catch`) en la API y regenere las rutas.

---

### Especificación para VS Code (`FIX_PREVIEW_GENERATION_SPEC.md`)

Crea o actualiza este archivo en la raíz del proyecto:

```markdown
# Corrección de Generación y Recuperación de Vista Previa

## Objetivo
Corregir la falla en el renderizado de la imagen de preview que activa el fallback por defecto, asegurando que Sharp genere correctamente la imagen de 450px con marca de agua sin romper la URL de la API.

---

## 1. Correcciones en la API de Imagen (`pages/api/preview/[key].ts` o `upload.ts`)

- Proteger el bloque de procesamiento de Sharp dentro de un `try/catch`.
- Si la imagen original no existe o falla la lectura, devolver una respuesta válida en formato WebP en lugar de romper el pipeline.
- Asegurar que el redimensionamiento a `450px` mantenga el formato correcto sin perder la transparencia del buffer original antes de aplicar el fondo.

```typescript
// Estructura segura en la API
try {
  const processedImage = await sharp(inputBuffer)
    .resize({ width: 450, fit: 'inside', withoutEnlargement: true })
    .flatten({ background: '#1E293B' }) // Aplica el fondo sólido
    .composite([{ input: watermarkBuffer, tile: true, blend: 'over' }])
    .webp({ quality: 80 })
    .toBuffer();

  return res.setHeader('Content-Type', 'image/webp').send(processedImage);
} catch (error) {
  console.error("Error procesando preview con Sharp:", error);
  // Retornar la imagen fallback o el buffer original en caso de fallo crítico
}

```

---

## 2. Verificación y Despliegue Automático

1. Validar compilación de TypeScript: `.\node_modules\.bin\tsc.cmd --noEmit`.
2. Ejecutar comandos Git automáticamente:
* `git add .`
* `git commit -m "fix: solucion a procesamiento de preview en sharp y manejo de errores"`
* `git push origin main`
