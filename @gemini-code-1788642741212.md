# Desacople de Supabase en Build Time para Despliegues en Vercel

## Objetivo
Evitar que el comando `next build` en Vercel falle por falta de variables de entorno al recopilar rutas estáticas o endpoints de administración (`/api/admin/ordenes`), permitiendo que el despliegue a `main` se ejecute con éxito.

---

## 1. Ajuste en Endpoints que Usan Supabase (`/api/admin/ordenes/route.ts` o equivalente)

- Agregar la directiva de renderizado dinámico en la parte superior del archivo para evitar que Next.js intente prerenderizar la ruta en tiempo de compilación sin variables de entorno:

```typescript
export const dynamic = 'force-dynamic';
export const revalidate = 0;