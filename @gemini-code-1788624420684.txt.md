Lee el archivo AUTOMATIC_DESIGN_UPLOAD_SPEC.md y aplica los cambios al formulario de subida:

1. Simplifica el modal de "Agregar nuevo diseño" dejando solo un campo de archivo: "Archivo de impresión (.png / .svg)".
2. Remueve la obligación de subir manualmente la "Vista previa (.webp)" y el "Diseño para Mockup".
3. En el endpoint de la API al procesar la subida, utiliza Sharp (o la librería de imágenes del proyecto) para generar automáticamente la preview ligera con marca de agua y el archivo comprimido para el visor de mockups.
4. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para verificar que compila sin errores.
5. Realiza git add, commit y push a origin main.