Lee el archivo THERMO_MATCH_SPEC.md y ajusta el termo para que coincida exactamente con la imagen de referencia:

1. En `lib/mockup-config.ts`, actualiza la configuración de `termo`:
   - Cambia `x` a `0.18`, `y` a `0.32`, `width` a `0.48` y `height` a `0.28`.
2. Confirma que se aplique la deformación cilíndrica con `curvature: 0.08`.
3. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para verificar que compila correctamente.
4. Realiza git add, commit y push a origin main.