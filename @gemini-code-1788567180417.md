Lee el archivo SHIRT_MOCKUP_POSITION_SPEC.md y ajusta el posicionamiento de las camisas:

1. En `lib/mockup-config.ts`, actualiza `printArea` para `camisa-negra` y `camisa-blanca`:
   - Mueve la posición vertical `y` a `0.20` (20%) para subir el logo al pecho alto.
   - Ajusta `x` a `0.35` (35%), `width` a `0.30` (30%) y `height` a `0.25` (25%).
2. En `components/MockupViewer.tsx`, asegúrate de renderizar el PNG con transparencia limpia para evitar cualquier recuadro de sombra/fondo alrededor del gráfico.
3. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para confirmar que compila sin errores.
4. Realiza git add, commit y push a origin main.