Lee el archivo MOCKUP_POSITION_FIX_SPEC.md y corrige el posicionamiento del Canvas:

1. Revisa `lib/mockup-config.ts` y ajusta las propiedades `x`, `y`, `width` y `height` de `printArea` para Camisa Negra, Camisa Blanca, Gorra, Taza y Termo para que queden centradas sobre el producto.
2. En `components/MockupViewer.tsx`, asegúrate de que el logo asignado en `diseno_mockup_url` se escale proporcionalmente (aspect-fit) para llenar el `printArea` en el pecho/frente, en lugar de dibujarse sin escalar en una esquina.
3. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para confirmar que no hay errores de tipos.
4. Haz git add, commit y push a origin main.