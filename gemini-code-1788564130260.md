Lee el archivo SEPARATE_MOCKUP_ASSETS.md y realiza los siguientes cambios:

1. Reincorpora en `lib/mockup-config.ts` y en la galería de detalle (`components/ProductGallery.tsx`) las 5 opciones de mockup: Camisa Negra, Camisa Blanca, Gorra, Taza y Termo.
2. Modifica `MockupViewer.tsx` para que utilice preferentemente la propiedad `diseno_corte_url` (o `logo_url`) de los datos del producto para realizar el estampado Canvas, dejando `imagen_preview_url` exclusivamente para la visualización comercial de la hoja A3/A4.
3. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para asegurar compilación limpia.
4. Realiza commit y push automático a main para actualizar la web en Vercel.


Lee el archivo MOCKUP_SECURITY_SPEC.md y aplica los siguientes cambios:

1. Modifica los tipos e interfaces del producto para soportar la propiedad `diseno_mockup_url` (imagen de baja resolución ~112px aislada para el Canvas).
2. En `lib/mockup-config.ts` y `components/ProductGallery.tsx`, habilita las 5 opciones de mockup (Camisa Negra, Camisa Blanca, Gorra, Taza y Termo).
3. En `components/MockupViewer.tsx`, usa `product.diseno_mockup_url` (con fallback a `imagen_preview_url`) para realizar el estampado Canvas, garantizando que el diseño se ajuste a la zona de impresión.
4. Verifica que no haya errores de compilación con `.\node_modules\.bin\tsc.cmd --noEmit`.
5. Ejecuta git add, commit y push a la rama main para actualizar la web en Vercel automáticamente.