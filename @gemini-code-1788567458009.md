Lee el archivo CYLINDRICAL_MOCKUP_ENGINE_SPEC.md e implementa el renderizado cilíndrico para la Taza y el Termo:

1. En `components/MockupViewer.tsx`, crea la función `drawCylindricalImage` para proyectar el gráfico dividiéndolo en franjas verticales con compresión lateral (efecto cilindro 3D).
2. Agrega la propiedad `isCylindrical: true` y los parámetros de curvatura en `lib/mockup-config.ts` para las variantes `taza` y `termo`.
3. Aplica esta función cuando el producto seleccionado sea la Taza o el Termo.
4. Revisa la posición del Termo para asegurar que el logo no choque con el asa derecha.
5. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para validar que todo compile correctamente.
6. Haz git add, commit y push a origin main.