Lee el archivo THERMO_SIZE_FIX_SPEC.md y ajusta el tamaño del logo en el termo:

1. En `lib/mockup-config.ts`, actualiza la configuración de `termo`:
   - Cambia `width` a `0.45` y `height` a `0.30` para agrandar el logo.
   - Ajusta `x` a `0.20` y `y` a `0.35` para mantenerlo centrado en el cuerpo negro.
2. Ejecuta `.\node_modules\.bin\tsc.cmd --noEmit` para confirmar que compila sin errores.
3. Haz git add, commit y push a origin main.